---
- name: Atualiza cache do apt
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Instala utilitarios de diagnostico e auditoria
  ansible.builtin.apt:
    name:
      - dnsutils      # dig
      - iputils-ping  # ping
      - curl
      - nmap
      - rsyslog
    state: present

- name: Garante resolucao de nomes interna via /etc/hosts
  ansible.builtin.lineinfile:
    path: /etc/hosts
    regexp: "^{{ item.ip }}\\s"
    line: "{{ item.ip }} {{ item.nome }}"
    state: present
  loop: "{{ hosts_internos }}"

# A regexp acima casa pelo IP, entao trocar o IP de um no deixaria a linha
# antiga para tras (ex.: "192.168.56.13 db" apos o db migrar para .20).
- name: Remove entradas antigas com o mesmo nome e outro IP
  ansible.builtin.lineinfile:
    path: /etc/hosts
    regexp: "^(?!{{ item.ip | regex_escape }}\\s)\\S+\\s+{{ item.nome }}\\s*$"
    state: absent
  loop: "{{ hosts_internos }}"
